// document.addEventListener('DOMContentLoaded', () => {
//     // Automatically hide success alert after 2 seconds
//     const successAlert = document.getElementById('success-alert');
//     const errorAlert = document.getElementById('error-alert');

//     if (successAlert) {
//         setTimeout(() => {
//             successAlert.style.opacity = 0; // Smooth fade out
//             setTimeout(() => successAlert.remove(), 500); // Remove from DOM after fade-out
//         }, 2000);
//     }

//     if (errorAlert) {
//         setTimeout(() => {
//             errorAlert.style.opacity = 0; // Smooth fade out
//             setTimeout(() => errorAlert.remove(), 500); // Remove from DOM after fade-out
//         }, 2000);
//     }
// });